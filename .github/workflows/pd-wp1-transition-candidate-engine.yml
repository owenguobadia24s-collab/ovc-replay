name: PD-WP1 Transition and Candidate Engine

on:
  pull_request:
    paths:
      - "src/ovc/research_operations/pattern_discovery/**"
      - "contracts/research_operations/pattern_discovery/PATTERN_DISCOVERY_TRANSITION_AND_WINDOW_ENGINE_CONTRACT_v0_1.md"
      - "fixtures/research_operations/pattern_discovery/pd_wp1/**"
      - "tests/research_operations/pattern_discovery/**"
      - "registries/research_operations/pattern_discovery/PATTERN_DISCOVERY_IMPLEMENTATION_REGISTRY_v0_3.yaml"
      - "docs/releases/pattern-discovery-v0-3/pd-wp1/**"
      - ".github/workflows/pd-wp1-transition-candidate-engine.yml"
  workflow_dispatch:

jobs:
  pd-wp1:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run PD-WP1 focused tests
        env:
          PYTHONPATH: src
        run: python -m unittest tests.research_operations.pattern_discovery.test_pd_wp1_transition_candidate_engine

      - name: Run PD-G0 retained-boundary tests
        env:
          PYTHONPATH: src
        run: python -m unittest tests.research_operations.pattern_discovery.test_pd_g0_design_freeze

      - name: Run canonical repository tests
        env:
          PYTHONPATH: src
        run: |
          set +e
          python -m unittest discover -s tests -p 'test_*.py' > canonical-tests.log 2>&1
          status=$?
          cat canonical-tests.log
          exit $status

      - name: Upload canonical diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pd-wp1-canonical-tests
          path: canonical-tests.log
          if-no-files-found: error
