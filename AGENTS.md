# OVC repository instructions for Codex

## Court record and authority

- Repository state is the court record. Do not infer missing authority from chat, prototypes, design prose, tests, or an attractive render.
- Preserve current programme authority boundaries. A Skill or Codex workflow provides competence, never operator-reserved authority.
- Do not consume Validation, activate scientific selectors/families/events/semantics, publish canonically, or create probability/risk/exposure/trading/execution behaviour unless an exact current repository authority explicitly permits it.
- Reconcile current `main` before integrating a bounded packet; preserve historical decisions and supersession lineage.

## Research Console vNext visual convergence — binding

When work touches `apps/research_console_vnext/**`, its visual system, screenshot harnesses, or Research Console vNext presentation:

1. Read `design/research_console/visual_target/OVC_Research_Console_Visual_Target_and_Convergence_Contract_v1_0.md`.
2. Treat `design/research_console/visual_target/VT-REF-01.svg` through `VT-REF-04.svg` as canonical visual references according to `REFERENCE_MANIFEST.json`.
3. Use `.agents/skills/ovc-visual-convergence/SKILL.md`.
4. Preserve Investigate / Research / Evidence / Control, source ownership, read-only authority, FVT, missingness, denominators, QA, evidence identity and fail-honest degradation.
5. Do **not** restore chart-first product identity. Charts may be context instruments where the workbench requires them.
6. Do **not** stop after an ordinary refinement pass. Execute render → compare → rank discrepancies → correct → test → rerender until all blocking `VC-01..VC-18` criteria pass or a typed WP3E stop condition occurs.
7. “Improved”, “functional”, “tests pass”, and “components exist” are not visual completion states.
8. Never elevate generic card dashboards, equal-box galleries, sparse shells, raw HTML semantic demos, write affordances or detached evidence into a final candidate.
9. `RCN-RN-G3V` is a single final operator visual/product gate after convergence, not a recurring critique loop.
10. No `WP4/G4` real-source work may begin while `RCN-RN-WP3E` is open.

## Assurance

- Prefer coherent correction batches over one-off pixel tweaks.
- After visual changes, run the relevant frontend/component tests and exact render checks before proceeding.
- Before final G3V readiness, verify 1920×1080, 1440×810 and 1280×720 and run full repository assurance required by the current programme.
- Preserve a discrepancy ledger; do not hide residual blockers behind a percentage score.
