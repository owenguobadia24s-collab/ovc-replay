# Research Console vNext — Codex override

These rules apply in addition to the repository root `AGENTS.md`.

- The binding visual target is `../../design/research_console/visual_target/OVC_Research_Console_Visual_Target_and_Convergence_Contract_v1_0.md`.
- Always use the repository skill `../../.agents/skills/ovc-visual-convergence/SKILL.md` for WP3E visual work.
- Start by rendering the exact current head before choosing changes. Compare composition and hierarchy before micro-polish.
- Fix the highest-impact coherent discrepancy batch first: shell geometry → primary dominance → panel density → semantic instrument quality → evidence integration → chrome/typography → responsive safety.
- Keep semantics source-bound. React may render and organize typed data; it may not invent scientific values or repair missing upstream facts.
- Continue refinement autonomously. Ordinary visual mismatch is work to fix, not an operator stop.
- Final readiness requires `VC-01..VC-18` PASS and two consecutive exact-head passes without blocking regression.
