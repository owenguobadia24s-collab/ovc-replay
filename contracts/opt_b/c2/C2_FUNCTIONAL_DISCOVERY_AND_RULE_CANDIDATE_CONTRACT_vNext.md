# C2 Functional Discovery and Declarative Research-Rule Candidate Contract vNext

Status: `CANDIDATE_METHOD_NOT_ADMITTED_PENDING_CEAR_G10`  
Authority: provisional, inactive, noncanonical research evidence only.

## 1. Construction order

The only lawful Part 10 construction order is:

`SHADOW_FROZEN C2 INPUTS -> COMPLETE NEUTRAL OPPORTUNITIES -> NEUTRAL FINGERPRINTS -> PROVISIONAL MOTIFS -> PROVISIONAL FAMILIES -> FUNCTIONAL CORES -> DECLARATIVE RESEARCH-RULE CANDIDATES -> COMPLETE STRUCTURAL EVALUATION -> LEGACY BENCHMARK COMPARISON -> OPERATOR DISPOSITIONS`

A legacy rule, historical CandidateWindow, outcome field or retrospective detector may not enter any dependency before the legacy benchmark-comparison stage.

## 2. Discovery-view registry

Every discovery view declares a versioned identity, clocks, sides, frames, object families, continuity policy, sequence lengths, required measurements, permitted fields and prohibited fields. It is inactive and noncanonical. Changes create a new version.

The registered view must explicitly prohibit outcome, future, profitability, MFE/MAE, probability, risk, exposure, trade, execution, Validation, retrospective detector, active-selector, semantic label, legacy-trigger and CandidateWindow fields.

## 3. Complete opportunity population

The opportunity builder receives a registered frozen-input scope and exact input-manifest hash. It emits exactly one record per requested source unit. Every record carries one explicit outcome:

- `APPLICABLE`
- `NOT_APPLICABLE`
- `COMPUTABLE`
- `NOT_EVALUABLE`
- `CENSORED`
- `CONFLICT`
- `POLICY_UNRESOLVED`
- `AUTHORITY_BLOCKED`

No queue, novelty, priority, capacity or review policy may change opportunity identity or population membership. Missingness, conflict, censorship and denied authority remain visible and cannot be omitted.

## 4. Neutral fingerprints

Only `COMPUTABLE` opportunities produce fingerprints. Fingerprints contain the starting condition, ordered development, ending structural effect, duration, path geometry, exact object and context identities, missingness and assurance. They contain no outcome, future, profitability, Validation, semantic or legacy-rule fields.

Fingerprint identity is deterministic from the versioned method pack and exact neutral features.

## 5. Motifs and provisional families

Motif extraction uses a preregistered token projection and minimum-support rule. Every fingerprint is accounted either in a retained motif or an explicit insufficient-support negative record.

Provisional-family construction uses a declared distance, deterministic lexicographic medoid order and full membership evidence. Motifs and families remain `PROVISIONAL_DISCOVERY`; recurrence alone grants no event, episode, semantic, selector or outcome authority.

## 6. Functional cores

Every provisional family yields an inspectable component matrix. Components are classified as:

- `INVARIANT`
- `COMMON`
- `OPTIONAL`
- `RARE`
- `CONTRADICTORY`

Thresholds are declared in the method pack. Functional cores have no semantic name and remain research candidates pending CEAR-G10.

## 7. Restricted rule AST

Only retained functional cores may be compiled. The allowed operators are:

- `ALL_OF`
- `ANY_OF`
- `SEQUENCE`
- `WITHIN_N_OBSERVATIONS`
- `SAME_OBJECT`
- `MEASUREMENT_COMPARISON`
- `RELATION_TRANSITION`
- `RUN_LENGTH`
- `CONTEXT_AVAILABILITY`

Compiled rules bind exact source opportunities, fingerprints, family, functional core, method pack and first-valid completion rule. They remain inactive, noncanonical and carry no selector, event, episode, semantic or outcome authority.

## 8. Evaluation and denominator completeness

Every compiled candidate is evaluated over the complete registered population. Each opportunity receives one result:

`MATCHED`, `NOT_MATCHED`, `NOT_EVALUABLE`, `CENSORED`, `NOT_APPLICABLE`, `CONFLICT`, `POLICY_UNRESOLVED` or `AUTHORITY_BLOCKED`.

Every numerator reconciles to the CEAR-G9 denominator and overlap policy. Raw counts and negative results are preserved.

## 9. Matched controls

Matched controls use the same eligible starting condition, clock, side, frame, object family, computability state and declared duration bin. Selection is lexicographic within an exact preregistered stratum. No nearest, best, dominant or fallback selection exists. Unmatched requests remain explicit negative evidence.

## 10. Legacy benchmark isolation

Legacy rules and CandidateWindow logic are evaluated only after bottom-up candidates are complete. Their match populations may be compared and later dispositioned as `REDISCOVERED`, `SPLIT`, `MERGED`, `PARTIALLY_RECOVERED`, `NOT_RECOVERED` or `CONTRADICTED`.

Legacy definitions have zero upstream seed, filter, score, stopping or promotion effect.

## 11. Real-population requirement

Synthetic fixtures prove contracts and determinism only. They cannot satisfy CEAR-G10 acceptance or support functional/rule-candidate dispositions. CEAR-G10 requires a content-addressed, reproducible full population produced from the exact approved vNext `SHADOW_FROZEN` inputs and policies.

A legacy active-C2 replay, compact summary, manifest without payload bytes, sampled population or non-reproducible external artifact cannot substitute for the required vNext population.

## 12. Prohibited authority

This contract grants no method freeze, candidate PASS, research-consumer permission, selector, event, episode, semantic, C2E, C2.5, C3, OPT-C/OPT-D outcome, Validation, publication, probability, risk, exposure, trading, execution or agent-write authority. CEAR-G10 remains operator-required.
