# ESLI-WP10 C3 ESL Proposition / AST Conformance Contract v0.1

Status: inactive deterministic conformance only. Authority effect: NONE.

This contract extends the ESL inactive C3 reference bridge with explicit proposition types, dependency manifests, optional organisation/family/constraint clauses, append-only statement generations, deterministic AST/render lineage and explicit unresolved/unknown states.

`C3BridgeMaturity` remains `INACTIVE_REFERENCE` throughout WP10. `SHADOW_EVALUATION` and `PRODUCTION_GRAMMAR` require separate exact operator authority plus vocabulary binding; this packet cannot activate either state.

Active semantic term admission remains operator-reserved. `ADMITTED_ACTIVE` is rejected by the WP10 proposition API. Optional clause absence is represented as typed omission; missing required clause evidence is `UNRESOLVED`, never fabricated false evidence.

`C3ExplanationRecord` is non-canonical, has `authority=NON_CANONICAL`, is excluded from identity projection and is forbidden from `RenderTrace` or proposition/AST authority. Historical statement generations are append-only and predecessor-linked; prior ASTs/renderings are never rewritten in place.
