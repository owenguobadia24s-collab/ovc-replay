# ESLI-WP11 Research Operations / DMRP Bridge Contract v0.1

Status: inactive deterministic conformance only. Authority effect: NONE.

The bridge is read-only and `INTERFACE_ONLY`. It exposes exact ESL evidence and EvidenceFrontier references to DMRP Path 1 or Path 2, then may bind an already-lawful immutable `ResearchCandidateGeneration` to a distinct `StructuralTermCandidate` through `LanguageCandidateBinding`.

Path 1 and Path 2 provenance remain distinct until the upstream DMRP owner creates an immutable candidate generation. Cross-mode correspondence is evidence, not identity merge. Mechanism hypotheses remain owned by Research Operations and cannot be manufactured or promoted by ESL association evidence.

If a complete lawful DMRP runtime is not materialized, the bridge returns `FULL_RESEARCH_HANDOFF / DOWNSTREAM_RUNTIME_NOT_MATERIALIZED`; it does not fabricate candidate execution. Candidate freeze, semantic admission, Development/Validation and exposure remain separately governed.
