# OVC MG EI-WP1 — Revised-C2 Source Adapter Contract v0.2

## Supersession

This contract is the operator-approved forward supersession of `MG_EI_WP1_REVISED_C2_SOURCE_ADAPTER_CONTRACT_v0_1.md` under `EI-WP1-CID-G0: PASS_SUPERSEDE`. The v0.1 contract remains immutable historical court-record evidence and is not rewritten.

The sole identity correction is the accepted `C2VNEXT.JUNE.DISCOVERY.INPUT.v1` binding SHA-256. The v0.1 transcription omitted the pair `ef` after `126a703b89bf`; v0.2 restores the already-existing authoritative digest. This is `TRANSCRIPTION_CORRECTION_NO_SOURCE_IDENTITY_CHANGE`, not a new source identity.

## Purpose and authority

Define the read-only translation from a revised-C2 empirical state row into the existing market-grammar `C2LedgerInput` contract used by C2E. Authority remains inactive, noncanonical `SHADOW_EXPERIMENT`; GBPUSD only; 15M local evaluation; BID/ASK; `2H_A_L` parent context; exact already-accepted June evidence only. No provider intake, selector, canonical selection, semantic/family/candidate/theory promotion, C3 handoff, publication, Active Discovery/Development/Validation, probability, risk, exposure or execution authority is granted.

## Accepted evidence binding

Forward execution MUST use all four exact inherited identities:

- `binding_id = C2VNEXT.JUNE.DISCOVERY.INPUT.v1`
- `binding_sha256 = 126a703b89bfef8fc60a4beb1248b20b424621334c8fff254c122555e44663f8`
- `logical_population_sha256 = 3f1089e3a4eefe94147c8c2f912e77899e4ed21fe8b3b8b85993e47bf7151ee7`
- `integrated_package_sha256 = 150de1997be8801baa59db6d0fe98b11cb21a6b70525b908537aeb31bfd00cc3`
- `source_release_id = C2AR.INTEGRATED.SHADOW.PACKAGE.v1`

The malformed historical 62-character value `126a703b89bf8fc60a4beb1248b20b424621334c8fff254c122555e44663f8` MUST fail SHA-256 validation and MUST NOT be padded, guessed, reinterpreted or accepted by weakening validation.

## Source row and deterministic translation

All v0.1 row-shape, five-axis, computability-precedence, categorical state-key, transition/reset, forbidden-field, chronology, duplicate, deterministic ordering and diagnostic-metadata exclusion semantics are incorporated unchanged. Specifically: all five axes `LOCATION`, `MOTION`, `ORGANISATION`, `INTERACTION`, `QUALITY` are required; measurements never enter C2E state identity; non-evaluable states preserve explicit missingness; the adapter never invents semantic completion; unknown/forbidden fields fail closed; and duplicate record IDs or first-valid timestamps within exact side/scope fail closed.

The forward adapter identity is `MG-EI-WP1-REVISED-C2-ADAPTER-v0.2`.

## Preservation and correspondence

- v0.1 contract, fixtures, registry and evidence remain unchanged.
- v0.2 fixture and registry are the forward execution surfaces.
- `MG_EI_WP1_BINDING_IDENTITY_CORRESPONDENCE_v0_1.json` records the exact historical-to-authoritative relationship.
- The binding ID, source slice, population identity, integrated package identity, instrument, sides, clocks and chronology do not change.

## Rollback

Disable/supersede the v0.2 forward surface and return consumers to a blocked state while preserving v0.1 historical evidence and the `EI-WP1-CID-G0` operator decision. Never reactivate the malformed 62-character value as a valid SHA-256.
