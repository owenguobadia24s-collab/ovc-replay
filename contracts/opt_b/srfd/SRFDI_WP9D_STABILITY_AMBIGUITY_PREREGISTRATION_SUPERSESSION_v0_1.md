# SRFDI-WP9D Stability and Ambiguity Preregistration Supersession v0.1

## Purpose

Resolve `WP10_STABILITY_METRIC_RULE_UNBOUND` before any June scientific execution by materialising the already-declared SRFD stability metric names as exact executable numerator, denominator, correspondence, ambiguity and chronology rules.

## Authority

This packet is candidate construction only. It grants no June run authority, consumes no authority token, reads no June scientific output, grants no provider intake, does not unlock Validation 2025, and grants no selector, family, semantic, publication, probability, risk, exposure or execution authority.

## Exact supersession boundary

`SRFD_PREREGISTRATION_CANDIDATE_v0_4.json` supersedes frozen v0.3 only for `STABILITY_AND_AMBIGUITY_METRIC_EXECUTION_SPECIFICATION_ONLY`.

The following remain unchanged: research questions and falsifiers; representation grid and pack registry; distance grid; family methods and parameter ladders; strong-family and invariant-core rules; residual and missingness policies; frozen v0.3 segmentation execution specification and registry; source binding; population membership; T0 capacity; Validation lock.

## Metric rules

The authoritative candidate rule materialisation is `registries/research/srfd/stability_metric_specs_v0_4.json`. The preregistration must contain the identical metric set and specifications and bind the registry logical hash.

Ambiguity is an exact maximum-positive-Jaccard tie with all tied references retained and no forced winner. Cross-sensitivity survival compares only adjacent rungs while holding representation, distance, method and all other shared configuration fields fixed. Cross-method correspondence requires exact member-set identity and does not select a winning method. Chronological stability is a descriptive temporal-span measure over fixed half-open June halves and must never be described as an independent half-sample refit test.

All rates retain numerator and denominator. Zero denominators emit explicit null-rate reason codes. No hidden composite, epsilon, learned threshold, post-result pooling, best-rung choice or chronology repartition is permitted.

## Gate

The candidate may advance only to `SRFDI-G9D-FREEZE`. `PREREGISTRATION_FREEZE` is operator-reserved. If frozen, the currently authorized v0.3 June token is preserved as immutable unused evidence and becomes superseded for execution; June remains denied until a new exact `SRFDI-G-JUNE-AUTH` decision binds the frozen v0.4 hashes and implementation commit.

## Rollback

Before freeze, close or supersede this branch without merge. After freeze, any scientific change requires another additive preregistration version. Never mutate historical v0.2/v0.3 artifacts.
