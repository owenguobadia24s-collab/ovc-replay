# RCN PVS v0.2 — Phase 2 Presentation Conformance Contract v1

Programme `OVC-RC-VNEXT-GREENFIELD-v0.1`; phase `PVS2`; parent plan `OVC-RCN-RESEARCH-NATIVE-IMPLEMENTATION-PLAN-0.3-RATIFIED`.

## Binding order
1. Repository scientific/read-model/authority contracts own meaning and lawful availability.
2. Figma file `NUHzyFiLHRuDWOC0hBVYzc`, canonical manifest `269:2`, owns the frozen PVS v0.2 visual/component target.
3. React + TypeScript implements that target against source-owned fixture/read models.
4. Neither Figma nor React may invent, repair, promote or activate scientific state.

## Canonical visual bindings
Current-main handoff `269:705`; Workbench shell `52:1134`; rail/header/context/navigator/status `44:159`, `46:213`, `47:292`, `49:338`, `50:316`; inspector/dock/passport `64:277`, `65:71`, `56:259`; typed identity/authority `21:29`, `22:45`, `23:85`; proof/AST/graph `89:325`, `90:416`, `81:525`; degraded state `91:221`; controls `24:125..37:202`; tables/ledgers `66:37..74:665`; ESL R3-R8 `202:40..259:162`.

Canonical route masters: `/structure` `106:2`; `/research` `111:2`; `/evidence` `113:2`; `/control` `115:2`.

## Frozen semantics
`AVAILABLE`, `AUTHORISED`, and `ACTIVE` are independent. Effective time, FVT and cutoff are independent. `NOT_EVALUABLE` is not false or null. `CENSORED` is not terminated. QA PASS does not grant authority. Selection/navigation state does not grant scientific or operator authority. Filtering, paging, virtualisation and graph projection do not redefine denominators or canonical population. `CAPACITY_EXCEEDED` must be explicit; silent sampling is prohibited.

## Implementation constraints
- React 19 + TypeScript + React Router + TanStack Query + existing CSS-variable/CSS styling; no Tailwind dependency.
- Consume `--ovc-*` semantic tokens from `productionTokens.css`.
- Transport remains GET-only; no mutation method or write-like control.
- Frontend calculations may concern presentation/layout only; no scientific state synthesis.
- All production routes retain source identity, FVT, authority planes, missingness, denominator, QA/provenance and authority effect.
- 1920x1080 is canonical; 1440x810 preserves hierarchy with persistent inspector; 1280x720 uses controlled inspector drawer and overflow before semantic compression.
- Real-source presentation remains `DENIED_UNTIL_RCN_RN_G4`.

## WP4D isolation
Open PR #611 (`RCN-RN-WP4D`) is a separate C3 preparation packet. Phase 2 must not retarget, modify, merge, supersede or use it to imply C3 runtime materialisation. C3 presentation remains fixture-only or typed `NOT_MATERIALIZED` according to repository source census.

## Acceptance / rollback
Acceptance requires full Research Console CI, TypeScript, Vitest, Vite build and Chromium Playwright PASS; geometry/trust-spine PASS; QA PASS; zero blocking discrepancies; authority delta NONE. Rollback restores prior presentation wiring without changing source/scientific authority or the RCN-RN-G4 boundary.
