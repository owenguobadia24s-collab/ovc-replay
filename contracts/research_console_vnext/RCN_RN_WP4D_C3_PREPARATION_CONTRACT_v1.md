# RCN-RN-WP4D — C3 Semantic Graph Preparation Contract v1

Programme `OVC-RC-VNEXT-GREENFIELD-v0.1`; packet `RCN-RN-WP4D`; authority `AUTO_EXECUTABLE_PREPARATION_ONLY`.

Repository census is binding: `OPT-B.C3` has no materialized runtime namespace and is `TYPED_DEGRADED_STATE` with `UPSTREAM_OWNER_NOT_MATERIALIZED_AS_RUNTIME_NAMESPACE`. WP4D MUST NOT fabricate semantic graphs, typed AST nodes, declarative grammar, temporal reasoning, causal semantics, or substitute C2/C2E/C2.5 representations.

`GET /api/v1/c3/graph` may expose only fixture-only typed absence: owner `OPT-B.C3`, `availability=NOT_MATERIALIZED`, `runtime_owner_materialized=false`, `binding_state=PREPARED_NOT_BOUND`, `nodes=[]`, `edges=[]`, `semantic_synthesis=PROHIBITED`, `authority_effect=NONE`, `real_source_presentation=DENIED_PENDING_RCN_RN_G4`, gate `RCN-RN-G4`.

Validation MUST be denied before resource resolution; transport remains GET-only. No semantic/grammar activation or promotion, scientific inference, write path, Validation, publication, probability/risk/exposure/trading/execution authority. First real-source presentation remains reserved to G4.

PASS requires census consistency, empty typed absence, no semantic synthesis/substitution, GET-only transport, full Research Console CI, QA PASS. Rollback removes this preparation surface only.