# C2-CSM Historical Reference Generation Contract v0.1

Programme: `OVC-EML-C2S-SPTO-CONFORMANCE-PREREG-v0.1`  
Packet: `C2S-SPTOI-WP1`  
Authority: historical reference/conformance only; `ACTIVE_C2_AUTHORITY_NONE`.

## Purpose

Define the identity and admissible state of the frozen historical C2-CSM generation `P3-R5-T2-S2` without converting historical research artifacts into current C2 owner truth.

## Frozen generation

`P3-R5-T2-S2` denotes:

- `P3`: sparse structural-object formation/lifecycle;
- `R5`: independent boundary-role succession;
- `T2`: atomic + compound transition/development state;
- `S2`: compound-aware current structural-map snapshot and append-only succession ledger.

The generation is `DESCRIPTIVE_REFERENCE_ONLY`. It MUST always expose `ACTIVE_C2_AUTHORITY_NONE` and MUST NOT substitute for the current owner-authoritative C2 vNext stream.

## Source sufficiency

A historical semantic is implementation-bearing only when its exact source contract/code/equivalent repository artifact is recoverable and content-identifiable. A journal entry, consumer copy, prose description, screenshot, later reconstruction, or `SOURCE_DERIVED` phase record MUST NOT define missing mechanics.

`REFERENCE_COMPLETE` requires exact implementation-bearing source coverage for every load-bearing P3/R5/T2/S2 semantic. Otherwise the generation is `REFERENCE_PARTIAL_SOURCE_LIMITED`. Partial status is lawful and MUST fail closed rather than reconstruct missing behavior.

## Historical evidence anchors

The historical corpus records 15 Development cases / 1,392 bars / 245 objects and 10 Holdout cases / 936 bars / 163 objects, for 25 cases / 2,328 bars / 408 objects. The frozen holdout result is 10/10 integrity PASS. These anchors are evidence/parity targets; they are not sufficient to define missing implementation semantics.

## Required invariants

- No source gap, reset or non-comparable discontinuity may be interpolated or bridged.
- Reference identities and serialized outputs MUST be deterministic and independent of worker/process/shard/cache/checkpoint identity.
- Exact source absence remains explicit.
- Current C2 owner authority always wins over the reference generation.
- A scientifically improved or repaired implementation is a new proposal outside this historical reference contract.
