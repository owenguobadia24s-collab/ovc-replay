# OVC Research Operations v0.3 — C1 Fact Assurance Authority Contract v0.1

Status: `FROZEN_AT_RO3_G0`

Plan: `OVC-RESEARCH-OPERATIONS-FOUNDATION-v0.3-C1-FACT-ASSURANCE-IMPLEMENTATION-PLAN-0.2`

## Granted boundary

RO3 may read exact remote-verified OPT-B.C1 Discovery and Development release records and their exact OPT-A parents. It may create replaceable local indexes, computability profiles, non-activating comparisons, QA evidence, read-only lineage traces and disabled Console projection adapters.

Allowed authority:

- `DESIGN_CANON_ONLY`
- `LOCAL_REPLACEABLE_DERIVED`
- `DERIVED_EXPLANATION_ONLY`
- `DERIVED_COMPARISON_ONLY`
- `QA_EVIDENCE_ONLY`
- `READ_ONLY_TRACE`
- `LOCAL_READ_ONLY_PRESENTATION_ADAPTERS`

## Source canon

- Discovery: `OPT-B.C1.GBPUSD.DISCOVERY.2021_2023.v1`
- Discovery manifest SHA-256: `6abd6d1fb74e7f3797e9add2435eaa5e487b612efd2f4b5f4f4c59679820d5d2`
- Development: `OPT-B.C1.GBPUSD.DEVELOPMENT.2024.v1`
- Development manifest SHA-256: `ca83f2d9d948be426f3d80ebc91cc981f92546dfdd07268d71938d618c51f017`
- Formula registry: `C1.FORMULAS.v0.1`, 18 formulas, exact Decimal arithmetic
- Record corpus: 212,764 records across 192 files

Source records, contracts, formula registry, releases, manifests and selectors remain immutable.

## One-way dependency

`OPT-A -> OPT-B.C1 -> RO3 derived evidence -> disabled Console adapters`

No reverse write is permitted. C2 and Pattern Discovery references are exact trace-only child references and cannot diagnose, score, tune or mutate downstream authority.

## Role access

Discovery and Development content may be resolved only with an explicit role. Validation is `LOCKED_UNCONSUMED`; denial occurs before path, object, record, timestamp or payload resolution. Validation C1 is `NOT_BUILT`.

## Mandatory denials

RO3 has no authority to:

- change any C1 equation, unit, domain, input, null rule, lookback, first-valid rule, symmetry rule, record schema, release or selector;
- rebuild, freeze, publish, supersede or activate a C1 release;
- read Validation content;
- create or tune C2, Pattern Discovery, C2E, C2.5 or C3 objects;
- introduce semantic candle names, thresholds, probability, risk, exposure, trading, execution or autonomous-agent writes;
- write Git primary branch or R2 directly from runtime code;
- include raw market data, full C1 streams, secrets, caches or machine paths in Git.

## Fail-closed law

Unknown role, release, manifest, formula, unit, null reason, clock, side, schema or authority vocabulary is blocking. Missing source identity or hash yields `NOT_EVALUATED`, never implied PASS. Null values remain evidence and are never replaced by zero, neutral or omission.

## Console boundary

RO3-G4 may accept adapters and fixture/source-bound projections only. Live Research Console C1 consumption remains disabled until a separate operator-owned `RC-G4` PASS. `RC-G4` is `OPERATOR_REQUIRED` and `NOT_AUTO_RATIFIABLE`.

## Rollback

Disable or remove RO3 projections and derived indexes while preserving all upstream OPT-A, C1, C2, Pattern Discovery and RPS authority and evidence. Never delete or mutate canonical releases or R2 objects and never reactivate legacy B-STATE authority.
