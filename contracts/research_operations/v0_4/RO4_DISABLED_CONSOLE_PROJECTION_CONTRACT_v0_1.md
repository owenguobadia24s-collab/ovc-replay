# RO4 Disabled C2 Sequence-Evidence Console Projection Contract v0.1

Status: `APPROVED_RO4_G5_DELEGATED`

Plan: `OVC-RESEARCH-OPERATIONS-FOUNDATION-v0.4-C2-STATE-SEQUENCE-EVIDENCE-IMPLEMENTATION-PLAN-0.2`

Packet: `RO4-WP5`
Gate: `RO4-G5`
Authority delta: `DISABLED_LOCAL_READ_ONLY_PRESENTATION_ADAPTERS`

This contract permits deterministic local construction and validation of the `ovc-ro4-console-projection/v1` object while the Research Console route remains `DISABLED_PENDING_RC_G5`.

The adapter may project only exact accepted C2 state and transition identities, persistence/conflict evidence, RO4 sequence evidence, read-only boundary/friction records, signature-diversity evidence, sample disclosure and Pattern Discovery trigger trace identities. Every count must show its eligible denominator and excluded/missing counts. Percentages, ratios, heatmaps, likelihood language and silent sampling are prohibited.

Operator-facing synthetic or ablated controls are prohibited. Pattern Discovery candidates, ranking, review-batch mutation and evidence-bridge writes are prohibited. Validation is `LOCKED_UNCONSUMED` and must fail before panel or record resolution.

The projection contains no actions, forms, buttons or write capabilities. `writes` is always `NONE`; remote deployment is always `DENIED`; no Console page or navigation route is registered by RO4-WP5.

Permanent banners must state that the route is disabled pending RC-G5, the surface is local read-only, annotation/write actions are unavailable, Validation is locked and C2/Pattern Discovery authority is unchanged.

Rollback removes or disables only the derived adapter and registry through a new non-destructive commit. RO4 evidence, append-only records, source C2 releases and all decisions remain preserved.
