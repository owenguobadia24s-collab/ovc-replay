# Shared Systems ESL shadow-consumer contract v0.1

Status: inactive `SHADOW_ONLY`. Authority effect: `NONE`.

SHSI-WP9 binds the exact completed `OVC-OPTB-ESL-CONFORMANCE-v0.1` state to
Shared Systems profile, EvidenceFrontier, lineage, interface, and read-model
surfaces without changing the ESL current binding. All mappings carry the complete
source record, declare zero loss, preserve a distinct source identity, and unwrap
exactly. They are inactive and cannot invent semantics.

The frontier crosswalk preserves dependency roles, missingness, source generations,
cutoff, first-valid chronology, and comparability. Missing required evidence yields
`NOT_EVALUABLE`. Missing optional enrichment yields `OPTIONAL_DEPENDENCY_MISSING`
while the lawful base structural record remains available; absence is never coerced
to zero or a placeholder.

Reference comparison covers exact ESL, C2E, SFC, and blocked C2.5 historical records.
No identity is rewritten. A no-family catalogue remains `NO_STABLE_FAMILY`; it does
not promote family or topology. C3 vocabulary activation remains immutable `NONE`.
The adapter ledger uses the frozen WP6 acceptance budget and blocks any miss.

This contract grants no C3 grammar/semantic activation, family or topology
promotion, source expansion, Validation, publication, scientific, probability,
risk, exposure, execution, or write authority.

Rollback: remove inactive ESL shadow bindings and preserve comparison evidence and
all existing ESL/C2E/SFC/C2.5 identities.
