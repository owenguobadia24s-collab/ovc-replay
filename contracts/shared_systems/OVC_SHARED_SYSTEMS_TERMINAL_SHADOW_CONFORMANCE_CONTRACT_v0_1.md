# Shared Systems terminal shadow-conformance contract v0.1

Status: non-cutover terminal reference contract. Authority effect: `NONE`.

SHSI-WP10 integrates exactly three shadow consumers—DSAI, Research Operations/DMRP,
and ESL—through exact ResolutionManifest and frozen SharedExecutionContext records.
All three remain `SHADOW_ONLY`; no current execution binding changes.

Integrated replay requires exact reference, optimized, restart, re-shard, and cold
resolution identity. Governed-corpus equivalence distinguishes already-lawful real,
governed historical, and synthetic-only evidence. Synthetic-only evidence can
complete shadow conformance but yields `DEFER` for adoption; it cannot authorize a
cutover.

The terminal pilot result covers all 18 frozen numeric dimensions and all nine
zero-tolerance floors. A cap miss or non-zero floor yields
`DEFER_OR_DO_NOT_MIGRATE`. Consumer adoption decisions are evidence-only packets for
a later separate operator gate and always require operator review. The terminal
Research Console projection is read-only, has no mutation routes, and performs no
frontend scientific calculation.

The terminal state is
`SHARED_SYSTEMS_V0_1_IMPLEMENTED_THREE_CONSUMER_SHADOW_CONFORMANT` only when there
are no unresolved incidents, owner conflicts, mandatory reachability gaps,
reproducibility failures, or current-binding changes. No scientific, semantic,
Validation, publication, probability, risk, exposure, execution, or cutover
authority is granted.
