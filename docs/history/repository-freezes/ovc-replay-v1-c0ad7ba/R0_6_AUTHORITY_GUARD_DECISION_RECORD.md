# R0-6 Repository Authority Guard Decision

Status: `EXECUTED_PASS`

The repository authority guard suite is now an active CI boundary. The six guard families prohibit quarantine imports, unreviewed package namespaces, legacy selector or release authority, old discovery seeds, reverse dependencies and raw market payloads in active Git storage planes.

Historical files under `docs/history/` and `legacy/quarantine/` remain available solely as non-authoritative court records. Their presence does not permit runtime use, selector fallback, release parentage, rollback, parameter sourcing or discovery seeding.

No provider intake, market replay, R2 publication, probability, exposure or execution authority is granted. The next bounded packet is R0-7 final validation and operator packet.
