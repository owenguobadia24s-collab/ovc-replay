# OVC C1 Wick-Balance Implementation Reconciliation and Immutable Release-Correction Programme v0.1

## Document control

- **Programme ID:** `OVC-C1-WICK-BALANCE-CORRECTIVE-PROGRAMME-0.1`
- **Operator instruction:** `RO3-WP3_RETEST_AFTER_C1_CORRECTIVE_PROGRAMME following steps 1 through 7`
- **Ratified:** 28 July 2026
- **Repository baseline:** `6c0aa91a6c51a86d39994ef363f8e29bb924764b`
- **Frozen formula authority:** `C1.FORMULAS.v0.1`
- **Corrective implementation identity:** `C1.IMPLEMENTATION.v0.2`
- **Current RO3 continuation:** `RO3-WP3_RETEST_AFTER_C1_CORRECTIVE_PROGRAMME`

## Decision

Reconcile the C1 implementation with the already-frozen formula registry without mutating that registry, reusing an immutable release identity, consuming Validation, or changing any selector during the auto-executable portion of the programme.

The blocking finding is a duplicate-implementation drift:

- the frozen registry defines `wick_balance = upper_wick_share - lower_wick_share`;
- `src/ovc/opt_b/c1/formulas.py` used the opposite sign;
- the historical full-corpus replay builder used the frozen registry sign.

The programme must prove the exact active-release condition before deciding whether release-byte correction is required. Capability and evidence do not grant publication or selector authority.

## Seven-step execution

1. Determine the exact affected C1 record and file inventory.
2. Trace exact C2 and Pattern Discovery affected surfaces.
3. Correct the implementation to the frozen formula registry under `C1.IMPLEMENTATION.v0.2`.
4. Replay Discovery and Development under new immutable candidate release identities.
5. Run complete C1 QA and RO3 formula assurance.
6. Present separate operator gates for R2 publication, C1 selector replacement and downstream remediation.
7. Resume RO3-WP3 only after the corrected release chain is remotely verified and all required operator decisions are recorded.

## Work packets

### C1C-WP1 — Implementation reconciliation and impact-audit machinery

Authority delta: deterministic code, tests, audit tooling and compact repository evidence only.

Required outcomes:

- one callable owns the wick-balance equation;
- fixture, full-replay and prospective-compute paths use that callable;
- the frozen registry and schema remain unchanged;
- the full-corpus auditor reports active mismatches separately from counterfactual divergence under the defective helper;
- Validation remains unresolved and unconsumed;
- no release, selector, C2, Pattern Discovery or R2 mutation occurs.

Gate `C1C-G1` is auto-ratifiable when focused and full repository tests pass and QA recommends PASS.

### C1C-WP2 — Exact corpus replay, immutable candidates and assurance

Prerequisite: `C1C-G1` completed in main.

Authority delta: local candidate construction and replaceable evidence only.

Required inputs:

- exact OPT-A Discovery artifact `8625089938`;
- exact OPT-A Development artifact `8625090401`;
- exact active C1 Discovery artifact `8627405334`;
- exact active C1 Development artifact `8627405553`.

Candidate identities:

- `OPT-B.C1.GBPUSD.DISCOVERY.2021_2023.v2`
- `MANIFEST.C1.OPT-B.C1.GBPUSD.DISCOVERY.2021_2023.v2.r1`
- `OPT-B.C1.GBPUSD.DEVELOPMENT.2024.v2`
- `MANIFEST.C1.OPT-B.C1.GBPUSD.DEVELOPMENT.2024.v2.r1`

Required outcomes:

- exact record/file impact inventory;
- two deterministic corrected replays;
- byte-level comparison with the active v1 record shards;
- new immutable local candidate roots and manifests;
- complete C1 QA;
- RO3 formula assurance against the frozen independent invariant canon;
- exact C2 and Pattern Discovery remediation inventory;
- no publication, selector change or canonical append.

Gate `C1C-G2` is auto-ratifiable only for local candidate and evidence acceptance.

## Operator-reserved gates

### C1C-G3 — R2 publication

`OPERATOR_REQUIRED`, `NOT_AUTO_RATIFIABLE`.

Decision concerns publication of the exact v2 candidate bytes under new immutable identities. It must bind candidate hashes, source commit, object inventory, collision preflight, rollback and remote verification procedure.

### C1C-G4 — C1 selector replacement

`OPERATOR_REQUIRED`, `NOT_AUTO_RATIFIABLE`.

Decision concerns replacing active Discovery and Development C1 selectors with the remotely verified v2 releases. The transaction must be atomic and preserve rollback to the exact v1 selectors.

### C1C-G5 — Downstream remediation

`OPERATOR_REQUIRED`, `NOT_AUTO_RATIFIABLE`.

Decision concerns C2 identity replay/republication/reselection and invalidation plus rerun of noncanonical June 2026 Pilot Discovery outputs. It grants no C2 semantic, threshold, family, novelty, Validation, probability, exposure or execution change.

## Impact interpretation

The active C1 releases may already contain the registry-correct sign because they were generated by the independent full-corpus replay code. In that case:

- active affected C1 records and files are zero;
- the new v2 release is an immutable implementation-reconciliation release whose record shards are expected to be byte-identical to v1;
- publication and selector replacement still require operator gates because release identity changes;
- active C2 semantics remain unchanged, but a C1 release-ID replacement changes C2 identities and therefore requires separately governed downstream replay;
- any June 2026 prospective compute made with the defective helper is noncanonical and must be invalidated and rerun rather than promoted.

## Prohibitions

- Formula registry or frozen contract mutation
- Existing release overwrite or identity reuse
- Validation consumption
- C2 semantic or threshold change
- Pattern-family, cluster, novelty or theory promotion
- Canonical/R2 publication before C1C-G3
- Selector replacement before C1C-G4
- Downstream activation before C1C-G5
- Probability, risk, exposure, trading, execution or agent-write authority

## Rollback

Before publication, discard only the unpublished v2 candidate roots and rebuild from the exact approved inputs. After publication, preserve all immutable bytes. Selector rollback returns to the exact active v1 C1 releases. Downstream rollback sets affected C2 selectors to the operator-approved prior state; it never reactivates retired legacy B-state authority.
