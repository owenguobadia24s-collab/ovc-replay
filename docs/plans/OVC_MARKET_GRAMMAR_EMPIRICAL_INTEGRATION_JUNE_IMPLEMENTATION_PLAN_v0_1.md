# OVC Market-Grammar Empirical Integration — June
## Implementation Plan v0.1

**Programme ID:** `OVC-MARKET-GRAMMAR-EMPIRICAL-INTEGRATION-JUNE-v0.1`  
**Plan ID:** `OVC-MG-EI-JUNE-IMPLEMENTATION`  
**Version:** `0.1`  
**Parent authority:** operator `PASS` at `MG-WP10`, recorded in `docs/releases/c2e-c2g-c2p-market-grammar-v0-1/mg-wp10/MG_WP10_OPERATOR_DECISION.json`.  
**Baseline:** latest lawful `main` after MG-WP10 terminal completion receipt.  
**Authority:** bounded, inactive, noncanonical `SHADOW_EXPERIMENT` empirical integration only.

## 1. Purpose

Replace the two mocked MG-WP8 topology boundaries with reproducible read-only adapters over the exact already-accepted GBPUSD June revised-C2 evidence, then exercise the existing C2E → C2G → 15M/2H_A_L context → typed C2P grammar stack end to end. The programme exists to establish empirical reproducibility and diagnostic comparison evidence. It does not select a canonical sensitivity, family, variant, rule, candidate or grammar.

## 2. Immutable authority envelope

Permitted:
- read the exact accepted evidence bound by `C2VNEXT.JUNE.DISCOVERY.INPUT.v1`;
- use GBPUSD only;
- use the already accepted 2026-05-30 through 2026-07-03 source slice and its June discovery population only;
- use 15M evaluation and `2H_A_L` context only;
- materialise replaceable local/shadow revised-C2 intermediate records;
- implement deterministic read-only adapters into C2E/C2G;
- run sensitivity comparisons using already registered noncanonical packs;
- run two clean builds and one restart verification;
- produce compact Git evidence, QA, comparison summaries, hashes and review surfaces.

Prohibited without a new operator decision:
- provider download or any new market-data intake;
- adding an instrument, market, side or clock;
- selector activation/replacement;
- `ACTIVE_DISCOVERY`, `ACTIVE_DEVELOPMENT` or `ACTIVE_VALIDATION`;
- canonical sensitivity/family/variant/rule/candidate/grammar selection;
- semantic/model/family/candidate/theory promotion;
- C3 handoff;
- canonical/R2 publication or new immutable release identity;
- probability, risk, exposure or execution authority;
- destructive action, history rewriting or force-push.

## 3. Exact external evidence lock

The programme must reject any source bytes that fail these identities:

| Object | SHA-256 | Bytes |
|---|---|---:|
| `GBPUSD_M1_BID_20260530_20260703_UTC.csv` | `d704b3bac2d51e839505ee5bc2ba7589ce44310353bbb6a464a1850fe1af5789` | 2,164,613 |
| `GBPUSD_M1_ASK_20260530_20260703_UTC.csv` | `bc643e62ebbc35940f93aaaaead147b6f9170b1a030a473436b2dc84d6992057` | 2,165,135 |
| `GBPUSD_H1_BID_20260530_20260703_UTC.csv` | `b6116ea784be785089e881c8b8a69c8d5d202cb57f3d058f71d159071bd51c24` | 35,893 |
| `GBPUSD_H1_ASK_20260530_20260703_UTC.csv` | `a25dcf89cb35afbbc6fb722e7f379511ceb83af0a5f1fc1b32c94e8adc304e5d` | 36,025 |

Additional bindings:
- binding ID: `C2VNEXT.JUNE.DISCOVERY.INPUT.v1`;
- binding SHA-256: `126a703b89bfef8fc60a4beb1248b20b424621334c8fff254c122555e44663f8`;
- logical population SHA-256: `3f1089e3a4eefe94147c8c2f912e77899e4ed21fe8b3b8b85993e47bf7151ee7`;
- integrated revised-C2 package SHA-256: `150de1997be8801baa59db6d0fe98b11cb21a6b70525b908537aeb31bfd00cc3`;
- requested opportunities: 33,320 = 27,996 computable + 1,638 censored + 3,686 not evaluable.

No regenerated bytes may be substituted under these identities.

## 4. Mocked boundaries to remove

MG-WP8 proved component topology but used two synthetic surfaces:
1. `c2_records`: the revised-C2 source boundary;
2. `state_structural_features`: the C2-to-C2G five-feature structural projection.

All downstream MG-WP2 through MG-WP7 component implementations remain the implementation baseline. Empirical adapters must preserve provenance as diagnostic-only data and must not introduce outcome/future/exposure fields.

## 5. Packet sequence

### EI-WP0 — governance and evidence preflight
Freeze this plan, authority/evidence contract, post-snapshot admission, exact Drive object identities, reproducibility receipt, programme state and tests. Exit: all repository tests PASS and no hash/source mismatch.

### EI-WP1 — real revised-C2 read-only source adapter
Build a deterministic adapter that resolves only the accepted binding/package and produces the lawful revised-C2 records required by C2E. No provider access. Outputs are replaceable `SHADOW_EXPERIMENT` intermediates. Exit: schema, chronology, leakage, identity and deterministic fixture tests PASS.

### EI-WP2 — real C2-to-C2G structural projection
Replace `state_structural_features` fixture injection with a deterministic five-axis structural projection from accepted revised-C2 records. The projection contract must expose exact derivation and explicit NOT_EVALUABLE/missingness. Exit: provenance separation, feature-domain, determinism and negative tests PASS.

### EI-WP3 — empirical integrated replay
Run the real revised-C2 adapter → C2E → C2G state/transition/episode families → hierarchy/variants → 15M/2H_A_L alignment → typed grammar/C2P parse path on the accepted population. Perform two clean builds plus restart verification. Keep all packs noncanonical. Exit: logical outputs/hash inventories are reproducible and capacity contract passes.

### EI-WP4 — empirical comparison and closeout
Produce sensitivity/hierarchy/split-merge/variant/grammar/migration/context/counterexample evidence and the read-only review model. Classify what was evaluated, not evaluated, ambiguous or unavailable. If the programme can close with no authority delta, auto-ratify and complete it. If any canonical selection, promotion, selector/publication/C3 or active-authority proposal is recommended, materialise one operator gate and stop before that action.

## 6. Capacity and storage

- maximum runtime per empirical run: 14,400 seconds;
- maximum retained external output: 10 GiB;
- raw market data, full intermediate streams and caches remain outside Git;
- Git stores contracts, schemas, registries, compact manifests, tests, QA and decisions only.

Capacity excess is `BLOCKED` or `QUARANTINED`; tests may not be weakened to force completion.

## 7. Determinism and QA

Required for each executable packet:
- closed contracts/schemas and explicit fixtures;
- canonical serialization/hash rules independent of local path, machine and run time;
- chronology and no-future-data checks;
- no outcome/probability/risk/exposure/execution dependency;
- source hash and lineage verification;
- focused tests plus complete repository suite, FINAL_HEAD, compatibility, merge readiness and zero unresolved review threads;
- rollback and programme-state update.

## 8. Rollback

Each packet may be reverted independently while preserving the parent MG-WP0…MG-WP10 evidence and operator decision. Any empirical mismatch quarantines the derived packet; it never invalidates or rewrites the accepted source evidence. No rollback may reactivate a selector or grant reserved authority.
