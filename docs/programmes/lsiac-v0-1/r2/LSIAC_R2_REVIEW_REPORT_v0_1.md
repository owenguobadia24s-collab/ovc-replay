# LSIAC-R2 Repository Scientific Capability Gap & Laboratory Accession Review v0.1

**Programme:** `OVC-LSIAC-v0.1`  
**Packet:** `LSIAC-R2-REPOSITORY-CAPABILITY-GAP-ACCESSION-REVIEW`  
**Baseline main:** `b9f747212a35d8c84946930ff2dc565fd4518849`  
**Baseline tree:** `836c262e76e03393a57c8a616aa4e4ed0b71e550`  
**Status:** `DELEGATED_PASS / EFFECTIVE_ONLY_AFTER_FINAL_EXACT_HEAD_ASSURANCE_AND_LAWFUL_INTEGRATION`  
**Authority effect:** `NONE`

## 1. Review question

What persistent scientific machinery does current OVC main already contain, what durable Laboratory capability remains outside the repository, and what is the smallest repo-side inheritance required to support larger populations, separately authorised asset classes and multiclock research without converting every specialised experiment into permanent architecture?

## 2. Scope correction

R2 separates **research programmes** from **durable research machinery**.

A Representation Robustness study, memory study, pretransition study, OPGS blind evaluation or similar programme is normal Research Operations work. It is not itself an accession target. If such a study later produces a durable representation comparator, reusable compiler, observer faculty, negative doctrine or other persistent capability, that output can return to LSIAC.

GEN0001 and GEN0002 are not rewritten. Their preservation-only decisions remain historical court record. R2 is a post-Science-Resume successor review with a narrower capability-first question.

## 3. Repository finding: generic scale machinery already exists

R2 does **not** recommend another generic research runner.

IROF already provides the execution abstraction required by the operator's scale objective: `PopulationSpec`, `PipelineProfile`, typed `StageSpec` adapters, `AuthorityBindings`, deterministic DAG execution, semantic run identity, artifact reuse, checkpoint/restart, capacity-aware scheduling and Research Operations/QA receipts. Its current pointer is `COMPLETED / INACTIVE_INFRASTRUCTURE_AVAILABLE`.

Therefore the proper architecture is:

```text
separately authorised PopulationSpec
        |
        v
IROF execution / checkpoint / capacity plane
        |
        +--> owner C2 stream adapter
        |       |
        |       +--> RRSCG core stage
        |       +--> multiclock comparison stage
        |       +--> specialised protocol stages when needed
        |
        +--> Research Operations / QA / evidence
```

The missing piece is scientific source/capability binding, not orchestration.

## 4. P0 blocker: current C2 owner read surface

Current repository state already records the exact obstruction in `C2S-SPTOI-WP4`: the current C2 owner structural snapshot stream is not exposed through a public authorised consumer read surface. `OWNER_READ_SURFACE_REQUIRED` and `SOURCE_NOT_EVALUABLE` are blocking, and private-engine introspection/reconstruction is explicitly prohibited.

This is a general prerequisite for scalable RRSCG/SPTO-style research. LSIAC cannot manufacture C2 owner truth. R2 therefore records a `HANDOFF_REQUIREMENT` to the C2 owner.

The desired owner surface should be population-parameterised and clock-identifying, but creating it grants no new instrument, market, side, clock or provider authority.

## 5. RRSCG should be accessioned as machinery, not as a monolithic programme

### 5.1 R2 continuation-constraint kernel

RRSCG's durable conceptual and mechanical contribution is the representation-robust continuation constraint with explicit consensus, ambiguity/support and abstention behaviour. The R2 algorithm survived fresh independent exact-hash pressure testing. The 2018 scientific result remained officially `PARTIAL_REPLICATION` because of the frozen H2 objective defect, so R2 accession must preserve that claim cap.

**R2 disposition:** `ACCESSION_CANDIDATE / CANONICAL_CAPABILITY`.

### 5.2 D9 observer-state dynamics

D9 is materially stronger as an implementation candidate than a mere research note. The source-bound record reports implementation tests, parent-R2 differential parity, full-chain synthetic assurance, tamper rejection, installable-wheel proof, exact 2018 source binding, denominator reconciliation and parent parity. Its real-source use remains descriptive Development characterisation only.

D9 gives OVC the reusable ability to represent a continuation constraint as a state field with geometry and kinematics. This is exactly the kind of observer faculty that becomes much more useful repo-side, where it can be replayed over much larger populations.

**R2 disposition:** `HIGH_PRIORITY_ACCESSION_CANDIDATE / OBSERVER_FACULTY`.

### 5.3 D10 reducer

D10 earned a fresh 2017 reducer comparison and exact-byte review. Its supersession is narrow: it replaces D9 forward at the reducer layer only. It does not erase the D9 dynamics machinery.

**R2 disposition:** `ACCESSION_CANDIDATE / CANONICAL_CAPABILITY / REDUCER_SUBCOMPONENT`.

## 6. Multiclock inheritance

The Laboratory established that clock is an observer coordinate. It also established what must **not** be inferred:

- 15M and `TV120_NATIVE` being clock-covariant does not create one shared categorical state/phase lattice;
- temporal nesting and morphology resemblance do not establish a stable compositional hierarchy;
- the tested categorical correspondence chain did not reproduce;
- the tested common continuous geometry did not add conditional information, but that negative is carrier-scoped rather than proof that multiscale information is absent;
- `TV120_NATIVE` is not silently equal to canonical `2H_A_L`.

The reusable inheritance is therefore a clock-indexed alignment/correspondence capability plus its negative doctrine, not a multiclock phase ontology.

**R2 disposition:** `ACCESSION_CANDIDATE / CANONICAL_CAPABILITY` for coordinate/alignment machinery and `RETAIN_FORWARD / NEGATIVE_KNOWLEDGE` for the claim caps.

## 7. Specialised research transport

R2 finds no reason to create a permanent repository layer for every specialist question. The default should be:

```text
specialised question
 -> frozen ResearchProtocol
 -> exact source/authority bindings
 -> IROF StageSpec/adapter
 -> bounded run over required population(s)
 -> Research Operations evidence
 -> discard/reuse adapter as appropriate
```

Only when a specialised line produces a generally reusable faculty or invariant should it become a future accession candidate.

This keeps the repository scientific core small while allowing research breadth to increase dramatically.

## 8. Minimal loading sequence

1. **Owner-source enablement:** obtain the public authorised current-C2 structural snapshot/read contract already required by C2S-SPTOI-WP4.
2. **RRSCG core accession:** build one versioned inactive RRSCG capability containing R2 constraint generation, D9 observer-state/dynamics and the narrow D10 reducer, with exact historical reference/parity fixtures.
3. **Single-clock scale proof:** prove deterministic long-population replay, checkpoint/restart and capacity behaviour through IROF before expanding source scope.
4. **Multiclock utility:** add clock-indexed alignment/correspondence machinery and negative-knowledge assertions; prove parity per clock without constructing a shared categorical ontology.
5. **Population expansion only under owner authority:** separately admit additional clocks, instruments, asset classes or providers when required by a frozen study. Repository machinery should be parameterised for this, but R2 does not grant those sources.
6. **Specialist transport:** move specialised programmes into the repo execution plane only when scale or population breadth warrants it.

## 9. R2 scientific/architectural conclusion

The repository does **not** principally lack a new research framework. It lacks a small set of source-bound scientific faculties sitting on top of infrastructure that already exists.

The highest-value inheritance path is therefore:

```text
CURRENT C2 OWNER READ SURFACE
        -> RRSCG R2 constraint kernel
        -> D9 observer-state geometry/kinematics
        -> D10 reducer subcomponent
        -> generic multiclock coordinate/alignment utility
        -> IROF for long/cross-population execution
```

Everything else remains specialised Research Operations work unless and until it produces a durable capability of its own.

## 10. QA and closeout

The R2 static review passed. The first permanent-PR workflow attempt correctly failed closed because detached exact-head VIT qualification had not yet been published. After lawful late-binding qualification was added, exact-head VIT routing, canonical pytest manifest, all four canonical pytest shards, the full repository suite, runner parity, pytest/unittest parity and tiered FINAL_HEAD assurance passed on the assured predecessor closeout head. The final decision/state materialisation changes the PR head and therefore must itself receive fresh detached VIT qualification and the required exact-final-head assurance before R2 becomes effective.

The closeout decision is `PASS` under delegated non-reserved authority with `NONE_CAPABILITY_GAP_REVIEW_ONLY` authority delta.

## 11. Authority boundary

This review creates no C2 source contract, no RRSCG activation, no new provider/instrument/market/side/clock authority, no active Discovery/Development/Validation role, no semantic/family/candidate/model/theory promotion, no C2E boundary authority, no SFF probability, no publication and no exposure/execution authority.

The first genuine blocker is the owner-controlled C2 read surface. The smallest lawful next route is an owner handoff packet, followed by an independently reviewed RRSCG core accession/conformance plan.
