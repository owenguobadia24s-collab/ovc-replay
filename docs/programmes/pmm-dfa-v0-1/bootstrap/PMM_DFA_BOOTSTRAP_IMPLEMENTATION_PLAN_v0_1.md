# PMM-DFA Bootstrap & R1T Gate Materialisation Plan v0.1

Programme: `OVC-PMM-0001`  
Packet: `PMM-DFA-BOOTSTRAP-0001`  
Next operator gate: `PMM-DFA-R1T-G0`  
Authority source: operator instruction to place a bounded PMM-DFA bootstrap/materialisation packet under repository governance.

## Purpose

Materialise the previously external/chat-side PMM Developmental Form Atlas lineage into repository governance without granting candidate freeze, Development access, Validation access, semantic promotion, publication, prediction, probability, risk, exposure, trading or execution authority.

The bootstrap binds the source-bound Laboratory/Drive controls already in custody, records the R1T six-object challenge pack as proposal-only, establishes a machine-readable programme state, and makes `PMM-DFA-R1T-G0` the next explicit operator-required gate.

## Governing evidence boundary

The repository did not previously contain a PMM-DFA programme/state/gate lineage. This packet therefore does not pretend that chat history was repository authority. It binds only externally accessioned source artifacts with explicit identities and preserves the chat-side R1T objects as proposals pending operator freeze.

The governing PMM programme is `OVC-PMM-0001`. The source-bound synthesis through R1T is `OVC-PMM-DFA-FORMAL-SYNTHESIS-MONOGRAPH-0.1`, Catalogue row 1243, SHA-256 `2d1e63a9d4eec7060306af4e97e6e766ea0a0902040b57b159cb6442cf074cc8`, filed/verified in the OVC Laboratory.

## Scope

This packet may:

- create repository programme/control records;
- bind exact external artifact identities and Laboratory catalogue references;
- materialise the six R1T candidate definitions as `PROPOSAL_ONLY_NOT_FROZEN`;
- materialise the R1T representation challenge envelope and Development acceptance design;
- create repository tests for the bootstrap invariants;
- prepare, but not approve, `PMM-DFA-R1T-G0`.

This packet may not:

- freeze or promote any candidate generation;
- access or reserve an untouched Development population;
- activate Development or Validation;
- alter the frozen C0B reference Atlas;
- publish a new immutable Atlas generation;
- activate probability, risk, exposure, trading or execution authority.

## Acceptance for bootstrap completion

1. machine-readable JSON parses;
2. source-binding hashes match independently verified Laboratory/Drive identities;
3. the R1T proposal contains exactly six unique proposal-only candidates;
4. `candidate_freeze == NONE` and `development_access == LOCKED_UNCONSUMED`;
5. `PMM-DFA-R1T-G0` is `OPERATOR_REQUIRED` and not approved;
6. no semantic/predictive/trading authority is granted;
7. rollback is forward-only and never rewrites source evidence.

## Post-bootstrap gate

`PMM-DFA-R1T-G0 — Novelty Candidate Generation & Development-Access Freeze` remains operator-reserved. A later `OVC RUN PMM-DFA-R1T-G0` may prepare its exact gate packet. Only `OVC APPROVE PMM-DFA-R1T-G0` may grant the reserved candidate-freeze and Development-access delta after exact source/population bindings and assurance conditions are satisfied.
