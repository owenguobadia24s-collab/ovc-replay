# SHSI-WP7 DSAI shadow-consumer materialisation

Status: implemented candidate pending exact-head repository assurance. Authority
delta: `NONE`. Runtime state: `SHADOW_ONLY`.

This packet binds the exact current DSAI programme to five inactive Shared Systems
surfaces: environment, run, assurance, receipt, and currentness. Each surface uses
the same reversible whole-record wrapper. Source and wrapper identities remain
separate, unwrap is exact, and field renaming, coercion, semantic invention, or
active adapter use is forbidden.

Historical dual-run replay agrees on every mandatory semantic path. DSAI and Shared
Systems both deny the protected security case without revealing metadata. Adapter
count remains zero, mapping count is one per wrapper, and measured code/byte overhead
is within the frozen WP6 acceptance budget.

The DSAI current binding, ORCH and merge paths, qualification and security contracts,
Validation lock, and all scientific, publication, probability, risk, exposure, and
execution authority remain unchanged.

Rollback: remove the inactive shadow manifest, wrappers, comparisons, and packet
projection. The pre-existing DSAI current path remains controlling throughout.
