# SHSI-WP9 ESL shadow-consumer materialisation

Status: implemented candidate pending exact-head repository assurance. Authority
delta: `NONE`. Runtime state: `SHADOW_ONLY`.

This packet binds the exact terminal ESL owner state to profile, EvidenceFrontier,
lineage, interface, and read-model surfaces through reversible whole-record mappings
with zero declared loss. Optional missing enrichment preserves the lawful base
structural record; missing required evidence becomes `NOT_EVALUABLE`.

Exact ESL, C2E, SFC, and blocked C2.5 identities are unchanged. No-family evidence
remains `NO_STABLE_FAMILY`, C3 vocabulary activation remains immutable `NONE`, and
no family, topology, source, or semantic promotion occurs. Adapter complexity stays
within the frozen WP6 budget.

Rollback: remove inactive ESL shadow projections while preserving owner records,
historical identities, missingness, and comparison evidence.
