# CEAR-G10 — Bottom-up Functional Discovery, Rule Compilation and Disposition

**Programme:** `OVC-C2-ANATOMY-REDESIGN-v0.2`  
**Plan:** `OVC-C2-ANATOMY-REDESIGN-IMPLEMENTATION / 0.3-REVISED`  
**Packet:** `C2AR-WP10`  
**Baseline main:** `8ff30900da9af11a2defe612fa6b1f0e86fb7a5f`  
**PR:** `#319`  
**Status:** **BLOCKED BEFORE DISPOSITION**  
**QA recommendation:** **BLOCK**

## Completed preparation

The branch implements the reusable, outcome-free Part 10 machinery:

- a versioned inactive discovery-view and method-pack candidate;
- exactly one explicit outcome for every requested opportunity;
- neutral sequence fingerprints;
- deterministic motifs and provisional families with medoids, members and insufficient-support negatives;
- inspectable functional-core matrices containing `INVARIANT`, `COMMON`, `OPTIONAL`, `RARE` and `CONTRADICTORY` components;
- a restricted declarative research-rule AST compiler and independent evaluator;
- exact-stratum matched controls with no nearest, best or fallback selection;
- post-discovery legacy benchmark comparison with zero upstream influence.

Synthetic execution is deterministic. It accounts for 14 requested opportunities, including all computable and negative outcomes; produces eight neutral fingerprints, two supported motifs, four insufficient-support negatives, two provisional families, two functional cores and two declarative rule candidates; and finds four exact-stratum matched controls. The complete repository suite and retained FINAL_HEAD checks pass on the pre-blocker implementation head.

These outputs are synthetic contract evidence only. They are explicitly ineligible for CEAR-G10 market-method or candidate dispositions.

## Blocking fact

CEAR-G10 requires a complete, content-addressed opportunity population produced from the approved revised C2 vNext `SHADOW_FROZEN` pipeline.

The repository contains a June full-month replay manifest, but it is bound to the legacy active release `OPT-B.C2.GBPUSD.DISCOVERY.2021_2023.v1`, not the revised vNext pipeline. It lists 22 external payload files and their hashes, but the payload bytes are not accessible through the repository evidence currently available. No revised vNext full-replay manifest, readable payload set, exact frozen-component binding, two-clean-run hash receipt or capacity/restart receipt was found.

The following substitutions are prohibited:

- treating legacy active-C2 v1 outputs as revised vNext outputs;
- reconstructing payload bytes from hashes;
- using synthetic fixtures to disposition market functions or rules;
- using sampled, top-k or preselected windows instead of the complete neutral population;
- allowing legacy rules to seed, filter, score, stop or promote discovery;
- adding outcome, Validation, profitability, risk, exposure or execution evidence.

## Gate-condition result

| Condition | Result |
|---|---|
| Complete opportunity population, denominators and deterministic method pack reconcile without legacy or outcome leakage | **BLOCKED** |
| Every retained functional/rule candidate has real lineage, controls, counterexamples and stability evidence | **NOT EVALUABLE** |
| Every method, candidate and benchmark mapping can receive an operator disposition | **NOT EVALUABLE** |
| No selector, event, episode, semantic, outcome, Validation or exposure authority is activated | **PASS** |

No discovery method, functional candidate, rule candidate, legacy mapping or research consumer is being dispositioned on this evidence.

## Smallest lawful resolution

Provide or produce one content-addressed, complete replay from the approved revised C2 vNext `SHADOW_FROZEN` pipeline. It must:

1. bind the exact source manifest, code commit, contracts, profiles, policies, calendar, clocks, sides and release identities;
2. make every declared payload byte readable;
3. include complete BID and ASK populations for registered 15M and `2H_A_L` discovery views;
4. prove no outcome, Validation or legacy-rule dependency enters the population;
5. run twice from clean workspaces and produce identical logical hashes;
6. record population reconciliation, capacity, checkpoint and restart evidence.

After that artifact is available, resume at `C2AR-WP10_INPUT_RESOLUTION_BEFORE_REAL_OPPORTUNITY_BUILD` with:

`OVC CONTINUE`

## Authority and rollback

Active C2 remains unchanged. No method admission, candidate PASS, consumer permission, selector, event, episode, semantic, outcome, publication, Validation, probability, risk, exposure, trading, execution or agent-write authority is granted.

PR #319 must remain unmerged. The branch, passing preparation code, synthetic evidence and blocker packet are retained. WP11 must not begin until the blocker is resolved and CEAR-G10 receives a complete lawful disposition packet.
