# CEAR-G7 — Transition Classification and Raw Detector-Policy Freeze

**Programme:** `OVC-C2-ANATOMY-REDESIGN-v0.2`  
**Packet:** `C2AR-WP7`  
**Gate class:** `OPERATOR_REQUIRED`  
**Recommended decision:** `PASS`  
**Exact approval command:** `OVC APPROVE CEAR-G7 PASS`

## Decision requested

Approve a frozen distinction between measurement, categorical, structural, reference-identity and computability changes, plus six inactive raw detector policies. Approval authorises contracts, schemas, deterministic implementation, fixtures, tests and read-only shadow outputs only.

### Transition classes

- `NO_CHANGE`
- `MEASUREMENT_CHANGE`
- `CATEGORICAL_CHANGE`
- `STRUCTURAL_CHANGE`
- `REFERENCE_IDENTITY_CHANGE`
- `COMPUTABILITY_CHANGE`

A transition may carry every applicable class and one primary class under a fixed evidence-severity order. The primary class carries no market meaning.

### Proposed raw detectors

- fixed-object directional crossing using ordered M1/tick evidence;
- source-precision touch;
- same-container entry and exit topology;
- raw absolute-distance increase/decrease/unchanged;
- reference-identity change, explicitly not crossing;
- structural graph additions, removals and supersessions.

## Current authority

The integrated observation/horizon/level/container/relation set and five formula profiles are `SHADOW_FROZEN`. All formula profiles are inactive and noncanonical. The active discovery selector, release and locked Validation boundary remain unchanged.

## Explicit non-authority

Approval does not grant detector activation or canonical selection, numeric thresholds, parameters, proximity scales or hysteresis; `APPROACHING`, `TESTING`, `REJECTING`, `ACCEPTING`, breakout, reversal, continuation, setup or signal semantics; event or episode promotion; parent resolver, denominator, rule or theory authority; provider intake, R2/canonical publication, Validation consumption, C2E/C2.5/C3, probability, risk, exposure, trading, execution or agent writes.

## Evidence boundary

Directional crossing requires the same immutable object identity and ordered evidence. OHLC span may only show coverage or path-order uncertainty. A changed reference is never rewritten as crossing. Missing, ambiguous or censored inputs fail closed without fallback.

## Warnings

No numeric persistence, acceptance, rejection, proximity or hysteresis threshold is selected. The proposed detectors emit raw evidence only. Parent-context resolver policy remains separately reserved for CEAR-G8.

## Rollback

Before approval, close or supersede the non-effective proposal. After approval, preserve accepted class and detector identities and use a new versioned supersession record for any change. Active C2 remains unchanged.

The machine-readable court record is `CEAR_G7_OPERATOR_DECISION_PACKET.json`.
