# FSR-WP0 — Repository and Authority Preflight / Topology Freeze

Programme: `OVC-FULL-STACK-SYNTHETIC-FRESH-DISCOVERY-REHEARSAL-v0.1`

Mode: `SYNTHETIC_ONLY / SHADOW_ONLY / NON_PROMOTABLE`

## Court-record baseline

- Repository: `owenguobadia24s-collab/ovc-replay`
- Latest lawful main pinned for this rehearsal: `f3b6c011ef2b7975340d0696025cd2da9b24b50e`
- Rehearsal branch: `test/full-stack-synthetic-fresh-discovery-v0.1`
- Branch was fast-forwarded from `41c2c8398ed781512b4ca07272e5cb842ab4b0f9` to the pinned main before any FSR implementation artifact was created.
- Preserved experimental blocker: PR #371 remains `PRESERVE_DO_NOT_MERGE`.
- Validation 2025 remains `LOCKED_UNCONSUMED`.
- No selector, threshold, representation, family, semantic, publication, probability, risk, exposure or execution authority is changed by this packet.

## Current descriptive pipeline topology

`SYNTHETIC SOURCE -> OPT-A fixture/intake machinery -> C1 -> revised C2 shadow topology -> state/transition/sequence evidence -> [C2E shadow if lawful] -> SRI/SRFD fixture-local benchmark surfaces -> [OccurrenceContext if executable] -> [C2P if executable] -> [revised C2.5 if executable] -> [C3/grammar shadow if executable] -> Research Operations / QA / read-model projection`

No higher layer may rewrite an upstream object or backdate `first_valid_time`.

## Implemented / not-implemented matrix

| Layer | Repository state at baseline | Synthetic/shadow rehearsal disposition |
|---|---|---|
| OPT-A v2 | Implemented; active sealed market-data machinery; Validation locked | `EXECUTE_VIA_GOVERNED_FIXTURE_ADAPTER_ONLY`; no provider intake or canonical release |
| OPT-B.C1 v2 | Implemented; deterministic atomic-fact engine | `EXECUTE_SYNTHETIC` |
| Active C2 v2 | `ACTIVE_DISCOVERY` for real Discovery release; Development reference-only | Do not mutate selector; synthetic rehearsal uses isolated fixture/shadow path only |
| Revised C2 / C2AR vNext | `IMPLEMENTED_SHADOW_COMPLETE`; observation, horizon, level, container, relation, axis, transition, parent-context, computability and functional-discovery machinery implemented | `EXECUTE_SYNTHETIC_SHADOW` |
| RO4 state/transition/sequence evidence | Implemented bounded local evidence layer | `EXECUTE_SYNTHETIC_READ_DERIVE` where adapters accept fixture outputs |
| C2E | Neutral episode ledger exists in inactive noncanonical `SHADOW_EXPERIMENT` market-grammar line; production activation remains denied | `EXECUTE_ONLY_IF_EXISTING_SYNTHETIC_SHADOW_ENTRYPOINT_ACCEPTS_FRESH_C2`; otherwise exact boundary + fallback |
| Structural Representation Interface / SRFD | SRFDI fixture/local benchmark implementation exists through WP9S; v0.2 preregistration frozen. Real June execution separately denied. Available preregistered packs include R1/R6/R8/R9; R2/R3/R4/R5/R7 are dependency-unavailable for the real-source preregistration | `EXECUTE_FIXTURE_LOCAL_ONLY`; no June/provider/Validation run; no method/family promotion |
| C2G / family surfaces | Inactive noncanonical market-grammar C2G sensitivity/hierarchy and family-variant/residual machinery exists; SRFD family methods are benchmark-only | `EXECUTE_SYNTHETIC_SHADOW/BENCHMARK_ONLY`; residual and unresolved outcomes preserved |
| OccurrenceContext | Architecture/interface exists; executable implementation must be verified before use | `VERIFY_ENTRYPOINT_THEN_EXECUTE_OR_MARK_NOT_IMPLEMENTED` |
| C2P persistent structural objects | Forward architecture defines C2P as persistent structural objects; no repository commit was identified that establishes a standalone production C2P implementation | `VERIFY_ENTRYPOINT`; if absent mark `NOT_IMPLEMENTED` |
| Revised C2.5 | Bounded design/plan and historical event machinery exist, but activation/promotion remains denied; no assumption of executable forward v0.2 event layer | `VERIFY_ENTRYPOINT`; execute only if already synthetic/shadow lawful |
| C3 | MTA decision remains `DEFER`; market-grammar line contains an inactive noncanonical typed grammar compiler/parser and migrated rule candidates, not canonical C3 authority | `EXECUTE_ONLY_AS_EXISTING_SHADOW_GRAMMAR_COMPONENT` and label as such; otherwise stop at boundary |
| Research Operations | Active bounded local append-only evidence, QA runner, artifact catalogue and read model; Console read-only | `EXECUTE_LOCAL_EVIDENCE/READ_ONLY` |
| MCARB | Programme completed scientific disposition; no automatic auxiliary-representation authority | No MCARB market benchmark; only consume existing lawful synthetic contracts if already embedded in an exercised fixture adapter |

## Authority matrix

Permitted by FSR programme:

- fresh synthetic fixture construction;
- deterministic fixture/local computation;
- existing fixture/shadow adapters;
- synthetic replay;
- research QA and compact evidence materialisation;
- read-only Research Operations / Console projection;
- benchmark comparison without promotion;
- in-scope integration defect correction without scientific threshold/semantic mutation.

Denied and retained:

- provider intake;
- ACTIVE_DISCOVERY / ACTIVE_DEVELOPMENT mutation;
- Validation consumption;
- selector activation/replacement;
- threshold, representation, family, sensitivity or semantic promotion;
- canonical/R2 publication under a new release identity;
- probability, risk, exposure, trading or execution;
- agent write authority.

## Blocked / reserved components

1. `SRFDI-G-JUNE-AUTH`: separately reserved; FSR does not invoke it.
2. PR #371: preserve/do-not-merge.
3. Real-source SRFD packs R2/R3/R4/R5/R7 remain dependency-unavailable under the frozen v0.2 preregistration; FSR may use separately lawful synthetic fixture benchmark paths but must not invent missing real-source dependencies.
4. Any C2P, revised C2.5, OccurrenceContext or C3 layer without a verified executable synthetic/shadow entrypoint is `NOT_IMPLEMENTED` or `AUTHORITY_BLOCKED`, never fabricated.
5. Validation 2025 remains `LOCKED_UNCONSUMED`.

## Rehearsal execution DAG

1. FSR-WP1: construct and seal a fresh deterministic synthetic BID/ASK source plus isolated hidden-construction ledger.
2. FSR-WP2: exercise OPT-A through lawful synthetic/fixture machinery; preserve gaps and first-valid chronology.
3. FSR-WP3: compute C1 atomic facts and deterministic rerun.
4. FSR-WP4: execute the complete revised-C2 shadow topology and selected human-readable lineage traces.
5. FSR-WP5: execute C2E only through an existing lawful synthetic-shadow entrypoint; otherwise record exact boundary and continue lawful fallback.
6. FSR-WP6: execute eligible SRI/SRFD/C2G synthetic benchmark paths; preserve method disagreement, residuals and unresolved outcomes.
7. FSR-WP7: verify/execute OccurrenceContext, C2P and revised C2.5 only where actual implementation + authority exist.
8. FSR-WP8: verify/execute the existing inactive grammar/AST surface only as its real implementation state permits; do not relabel a component as canonical C3.
9. FSR-WP9: materialise evidence envelope, run manifest, artifact catalogue, QA, lineage, missingness, authority and read-model projection.
10. FSR-WP10: clean-room rebuild from sealed synthetic source; compare every deterministic stage.
11. FSR-WP11: only after outputs are frozen, compare against hidden construction ledger.
12. FSR-WP12: acceptance dossier and one final classification.

## FSR-G0 decision

Acceptance conditions:

- latest lawful main pinned before FSR mutation: PASS;
- current authority and selector boundaries explicit: PASS;
- implementation gaps not treated as authority: PASS;
- Validation lock preserved: PASS;
- PR #371 preserved: PASS;
- no reserved authority crossed: PASS;
- execution DAG explicitly permits fallback rather than fabricated layers: PASS.

`FSR-G0 = PASS_AUTO_RATIFIED`

Authority delta: `NONE_RESERVED`; only the bounded synthetic rehearsal work already authorised by the FSR programme proceeds.

Next packet: `FSR-WP1 — Fresh synthetic source and sealed fixture`.
