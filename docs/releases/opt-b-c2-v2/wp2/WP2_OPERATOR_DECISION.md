# OPT-B.C2 v2 WP2 — contract freeze

**Decision: PASS — C2 contracts, schemas, registries, parameter pack, reason codes, QA mapping and synthetic handoff fixtures are frozen.**

WP3 level/container/relation engine implementation and WP4 parallel-state/transition engine implementation are authorised against these exact versions. Market replay remains denied until C2-G2 fixture trust and a separate operator approval. C2 release, selector and activation remain `NONE`; Validation is `LOCKED_UNCONSUMED`; probability, exposure, trading and execution remain `NONE`.

Any contract, schema, registry, threshold, level/container type or gap-policy change requires a new semantic version and full replay.