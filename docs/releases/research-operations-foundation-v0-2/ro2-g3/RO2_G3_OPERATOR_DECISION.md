# RO2-G3 operator decision

Decision: `PASS_LOCAL_READ_ONLY_PRESENTATION_ADAPTERS`

RO2-WP3 is accepted as a bounded local read-only presentation layer over the accepted RO2-G1 and RO2-G2 read models.

Accepted surfaces:

- workspace projection;
- quality projection;
- lineage projection;
- Discovery and Development admissible-cutoff replay projection;
- release and workspace comparison projection;
- stable deterministic panel and aggregate projection identities;
- Validation release identity and aggregate metadata only.

Validation remains `LOCKED_UNCONSUMED`. Validation content, paths, timestamps, object identities and rows remain denied before resolution.

No adapter may write to Git, R2, selectors, releases, thresholds, classifications, parameters, evidence authority or execution systems. Console projections remain replaceable presentation records subordinate to their source evidence and accepted RO2 read models.

The next boundary is RC-G3 consumption and must receive a separate operator instruction.
