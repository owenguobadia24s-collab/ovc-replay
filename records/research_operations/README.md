# Research Operations durable records

This root is reserved for compact, schema-valid and canonically serialized Research Operations records.

RO-WP1 creates no operator record and grants no durable write service. Full market payloads, large artifacts, databases, caches, drafts and machine-specific paths are prohibited here.

Derived local state belongs under `var/research_operations/`, which is Git-ignored and rebuildable.