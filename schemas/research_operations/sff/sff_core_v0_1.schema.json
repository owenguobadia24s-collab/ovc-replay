{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ovc-sff-core/v0.1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "identity", "cutoff_at", "owner_authority_id"],
  "properties": {
    "schema": {"const": "ovc-sff-core/v0.1"},
    "identity": {"type": "string", "minLength": 1},
    "cutoff_at": {"type": "string", "format": "date-time"},
    "owner_authority_id": {"type": "string", "minLength": 1}
  }
}
